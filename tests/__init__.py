"""
Test suite organization.

This package contains pytest test files organized by goal area. When adding a new test file
that covers multiple goal areas, place it in the subdirectory matching the largest number of
test functions for that area. Break ties by the subdirectory listed earliest in this docstring's
list: packaging, server, mcp_tools, indexing, config.

Record the placement rationale (areas considered + counts) in the new file's own module docstring.
"""
